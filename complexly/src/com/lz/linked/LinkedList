


LinkedList内部维护了Node内部类。
一个类对象就代表了一个元素。
注意头结点和尾结点的操作。（边界条件）

LinkedList 没有确定的大小，通过指针的移动来指向下一个的内存地址

LinkedList 每次添加元素只是改变元素的一个指针引用和下一个指针引用，没有扩容。
所以对比ArrayList需要扩容，而且在中间插入元素时，后面的所有元素都要移动一位。
两者插入元素的效率差异很大。 不能像数组结构根据索引获取元素。

